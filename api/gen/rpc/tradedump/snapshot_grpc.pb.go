// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v7.34.0
// source: rpc/tradedump/snapshot.proto

package tradedumprpc

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	TradeDumpSnapshot_TakeSnapshot_FullMethodName = "/opentrade.rpc.tradedump.TradeDumpSnapshot/TakeSnapshot"
)

// TradeDumpSnapshotClient is the client API for TradeDumpSnapshot service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type TradeDumpSnapshotClient interface {
	// TakeSnapshot asks trade-dump to produce a fresh per-vshard snapshot
	// that is guaranteed to include every record on the counter-journal
	// partition up to the current LEO. The returned snapshot_key is a
	// blob-store object Counter then downloads and restores (ADR-0064
	// §3 Phase 1 step ④–⑤).
	//
	// Semantics:
	//
	//   - Called exactly once per Counter startup, per vshard (no retry
	//     on the Counter side; failures fall back to the legacy
	//     "load last periodic snapshot + catchUpJournal" path).
	//   - trade-dump may coalesce concurrent calls for the same vshard
	//     via singleflight; callers see the same response in that case.
	//   - trade-dump enforces a global in-flight limit (default 16) to
	//     protect against whole-host restarts saturating the Capture
	//     machinery; over-limit callers receive RESOURCE_EXHAUSTED.
	//
	// Error codes:
	//
	//   - INVALID_ARGUMENT    vshard_id out of range [0, vshard_count)
	//   - FAILED_PRECONDITION requester_epoch older than the last seen
	//     epoch for this vshard (zombie-Counter
	//     defense; ADR-0058 vshard lock is the
	//     primary guard)
	//   - DEADLINE_EXCEEDED   shadow engine could not apply up to the
	//     partition's LEO within the wait budget
	//   - RESOURCE_EXHAUSTED  global in-flight limit reached
	//   - INTERNAL            shadow / capture / serialize error
	//   - UNAVAILABLE         Kafka metadata query (LEO) / blob-store
	//     upload failed, callers should fall back
	//   - UNIMPLEMENTED       handler wiring present but actual logic
	//     not yet built out (ADR-0064 M1b
	//     skeleton — replaced in M1c)
	TakeSnapshot(ctx context.Context, in *TakeSnapshotRequest, opts ...grpc.CallOption) (*TakeSnapshotResponse, error)
}

type tradeDumpSnapshotClient struct {
	cc grpc.ClientConnInterface
}

func NewTradeDumpSnapshotClient(cc grpc.ClientConnInterface) TradeDumpSnapshotClient {
	return &tradeDumpSnapshotClient{cc}
}

func (c *tradeDumpSnapshotClient) TakeSnapshot(ctx context.Context, in *TakeSnapshotRequest, opts ...grpc.CallOption) (*TakeSnapshotResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(TakeSnapshotResponse)
	err := c.cc.Invoke(ctx, TradeDumpSnapshot_TakeSnapshot_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// TradeDumpSnapshotServer is the server API for TradeDumpSnapshot service.
// All implementations must embed UnimplementedTradeDumpSnapshotServer
// for forward compatibility.
type TradeDumpSnapshotServer interface {
	// TakeSnapshot asks trade-dump to produce a fresh per-vshard snapshot
	// that is guaranteed to include every record on the counter-journal
	// partition up to the current LEO. The returned snapshot_key is a
	// blob-store object Counter then downloads and restores (ADR-0064
	// §3 Phase 1 step ④–⑤).
	//
	// Semantics:
	//
	//   - Called exactly once per Counter startup, per vshard (no retry
	//     on the Counter side; failures fall back to the legacy
	//     "load last periodic snapshot + catchUpJournal" path).
	//   - trade-dump may coalesce concurrent calls for the same vshard
	//     via singleflight; callers see the same response in that case.
	//   - trade-dump enforces a global in-flight limit (default 16) to
	//     protect against whole-host restarts saturating the Capture
	//     machinery; over-limit callers receive RESOURCE_EXHAUSTED.
	//
	// Error codes:
	//
	//   - INVALID_ARGUMENT    vshard_id out of range [0, vshard_count)
	//   - FAILED_PRECONDITION requester_epoch older than the last seen
	//     epoch for this vshard (zombie-Counter
	//     defense; ADR-0058 vshard lock is the
	//     primary guard)
	//   - DEADLINE_EXCEEDED   shadow engine could not apply up to the
	//     partition's LEO within the wait budget
	//   - RESOURCE_EXHAUSTED  global in-flight limit reached
	//   - INTERNAL            shadow / capture / serialize error
	//   - UNAVAILABLE         Kafka metadata query (LEO) / blob-store
	//     upload failed, callers should fall back
	//   - UNIMPLEMENTED       handler wiring present but actual logic
	//     not yet built out (ADR-0064 M1b
	//     skeleton — replaced in M1c)
	TakeSnapshot(context.Context, *TakeSnapshotRequest) (*TakeSnapshotResponse, error)
	mustEmbedUnimplementedTradeDumpSnapshotServer()
}

// UnimplementedTradeDumpSnapshotServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedTradeDumpSnapshotServer struct{}

func (UnimplementedTradeDumpSnapshotServer) TakeSnapshot(context.Context, *TakeSnapshotRequest) (*TakeSnapshotResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method TakeSnapshot not implemented")
}
func (UnimplementedTradeDumpSnapshotServer) mustEmbedUnimplementedTradeDumpSnapshotServer() {}
func (UnimplementedTradeDumpSnapshotServer) testEmbeddedByValue()                           {}

// UnsafeTradeDumpSnapshotServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to TradeDumpSnapshotServer will
// result in compilation errors.
type UnsafeTradeDumpSnapshotServer interface {
	mustEmbedUnimplementedTradeDumpSnapshotServer()
}

func RegisterTradeDumpSnapshotServer(s grpc.ServiceRegistrar, srv TradeDumpSnapshotServer) {
	// If the following call panics, it indicates UnimplementedTradeDumpSnapshotServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&TradeDumpSnapshot_ServiceDesc, srv)
}

func _TradeDumpSnapshot_TakeSnapshot_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(TakeSnapshotRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(TradeDumpSnapshotServer).TakeSnapshot(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: TradeDumpSnapshot_TakeSnapshot_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(TradeDumpSnapshotServer).TakeSnapshot(ctx, req.(*TakeSnapshotRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// TradeDumpSnapshot_ServiceDesc is the grpc.ServiceDesc for TradeDumpSnapshot service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var TradeDumpSnapshot_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "opentrade.rpc.tradedump.TradeDumpSnapshot",
	HandlerType: (*TradeDumpSnapshotServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "TakeSnapshot",
			Handler:    _TradeDumpSnapshot_TakeSnapshot_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "rpc/tradedump/snapshot.proto",
}
