# Research TODO

- [x] After ADR-0071 is implemented, re-verify ADR-0070 ADL cross-sequencer execution.
  - **Added 2026-05-31** from user review of ADR-0070 implementation. Current ADR-0070 code enters the counterparty user's sequencer synchronously, but the stricter protocol described in ADR-0070/0071 still needs a post-0071 audit: ADL tasks must carry `{user, symbol, qty, price, side, pos_seq/version, adl_round}`, and the owning shard/user sequencer must reject stale tasks when side/version/profitability no longer match. This TODO should be closed only after ADR-0071's coordinator/executor path proves that the old same-process hand-off cannot mutate a stale counterparty position.
  - **Resolved 2026-05-31** in ADR-0071 implementation: `perp-risk` now dispatches ADL tasks with `side`, `pos_seq`, `position_version`, and `adl_round`; the owning `perp-counter` user sequencer rejects side mismatch, stale `LastMatchSeq`, stale `Position.Version`, non-profitable/non-sacrificing tasks, and duplicate ADL rounds before mutating the position.
- [x] Confirm who is responsible for generating, triggering, and storing snapshots for the dual-output matching service architecture.
  - **Resolved 2026-04-26** in [ADR-0066](../adr/0066-trade-dump-projection-platform-admission.md): Match snapshot stays inside Match (self-produced, periodic + on symbol unload, ADR-0030). Rejected as a trade-dump shadow candidate because `order-event` is a command stream — replaying it would require duplicating the matching engine in trade-dump with no outsourcing benefit, and would couple every matching-rule upgrade to two binaries. The admission rule (state journal vs command stream) makes this a categorical decision, not a per-case judgment. Storage migration to S3/EFS for HA remains possible but is independent of producer ownership.
